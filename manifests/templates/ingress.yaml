---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springboottt                  # Ingress name
  labels:
    app: springboottt
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springboottt.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springboottt               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
